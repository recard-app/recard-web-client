import React from 'react';
import { CreditContent } from '../../../types/ChatTypes';

interface CreditBlockProps {
    content: CreditContent;
}

/**
 * Placeholder component for displaying credit information.
 * This will be implemented in a future phase.
 */
function CreditBlock({ content }: CreditBlockProps): React.ReactElement | null {
    return null;
}

export default CreditBlock;
