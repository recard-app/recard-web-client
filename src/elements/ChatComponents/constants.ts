/**
 * Chat Component Feature Toggles
 *
 * Dev-only toggles for switching between component variants.
 */

/** When true, renders the legacy V1 multiplier component instead of V2 */
export const USE_LEGACY_MULTIPLIER_COMPONENT = true;
